
- file 文件名
能看到文件64位还是32位
可执行文件还是动态库
架构(x86_64/ARM)
是否为静态链接/动态链接

- mount | grep vmhgfs
可以找找虚拟机和主机的共享文件地址

- realpath 文件名
显式指定文件的绝对路径

- strings -a 文件名 | grep /bin
查字符串中的文件

看自己是谁,权限
- whoami
- id
- groups
- pwd

看系统版本,内核
- uname -a
- cat /etc/os-release
- cat /etc/issue
- lsb_release -a

全盘搜索flag
- find / -name " * flag * " 2>/dev/null

看环境变量
- env
- export
- echo $PATH
- echo $HOME