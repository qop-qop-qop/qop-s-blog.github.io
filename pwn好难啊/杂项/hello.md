read函数不像write函数一样可以输出内容,而且read函数的第一个参数只接受fd

start有时可以有时不可以,main函数比较稳定

read和write不被\0截断

strlen可以被\x00截断

注意系统级别调用函数和普通的call 函数有很大不同,普通call的函数内部结尾有ret
但是系统级别调用结束后没有ret,直接跳到原rip下一条指令,这是后要结合汇编代码注意rbp和rsp的关系和位置

