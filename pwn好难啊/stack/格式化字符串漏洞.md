-  %d
输出10进制整数

- %c
输出字符

- %s
输出内存中的字符串,内存中存的是字符串所在的地址

- %x
输出十六进制数据

%%对于printf函数来说,我们输入的字符串输入printf的第一个参数,在rdi寄存器中,不过,如果我们在字符串中加上%1$p,那么这个1其实指代的是printf的第二个参数,即rsi,不过对于我们来说,偏移位一%%

用%p泄露的地址看起来没问题,但是用recv接收的时候要把这个字符串地址变成数字地址
canary = int(canary_str, 16)